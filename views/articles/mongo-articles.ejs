<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      <%= title %>
    </title>

    <link rel="icon" href="/favicon.ico" />
    <link rel="stylesheet" href="/styles.css" />
  </head>

  <body class="<%= theme %>">
    <div class="container">
      <h1>MongoDB Articles</h1>

      <div class="theme-switch">
        <a class="btn" href="/set-theme/light?redirect=/mongo-articles">Light</a>
        <a class="btn" href="/set-theme/dark?redirect=/mongo-articles">Dark</a>
      </div>

      <ul>
        <% articles.forEach(article=> { %>
          <li>
            <strong>
              <%= article.title %>
            </strong> — <%= article.description %>
          </li>
          <% }) %>
      </ul>

      <a href="/">Back to home</a>
    </div>
  </body>

</html>